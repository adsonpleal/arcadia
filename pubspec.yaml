name: arcadia
description: "A new Flutter project."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.10.8

dependencies:
  collection: ^1.19.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  meta: ^1.17.0

flutter:
  uses-material-design: true
