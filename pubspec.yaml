name: arcadia
description: "A new Flutter project."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.6.0-55.0.dev

dependencies:
  collection: ^1.18.0
  flutter:
    sdk: flutter
  macros: ^0.1.2-main.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Unfortunately we can't use flutter_test with macros.
  # TODO: remove this when macros start supporting flutter_test.
  test: ^1.25.8

flutter:
  uses-material-design: true
