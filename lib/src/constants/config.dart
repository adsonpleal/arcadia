/// Unit virtual pixel ratio.
///
/// Each unit takes 5 virtual pixels.
const unitVirtualPixelRatio = 5.0;

/// The tolerance for selection clicks and hover.
const selectionTolerance = 1;

/// Minimum drag distance in world units before selection drag starts.
const selectionDragStartDistance = 2.0;
